<?php
/*
	Configure the watchdog!
*/

$wait                           	= 30;	// time to wait beween upgrading the data, default 30 seconds
$changes_length_max			= 20;	// the maximum size of last changes cumulated on the last row
