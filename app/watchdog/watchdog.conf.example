<?php
/*
	Configure the watchdog!
*/

$wait                           	= 30;	// time to wait beween upgrading the data, default 60 seconds
$changes_length_max			= 30;	// the maximum size of last changes cumulated on the last row

ini_set('display_errors', 0);
error_reporting(0);
